from aocTools import getLines
dayData = "2a"


def process(fileName):
    count = 0
    for line in getLines(fileName):
        continue

    return count


assert process(f'd{dayData}.ex.data') == 31
process(f'd{dayData}.data')
